// Copy this file to config.h and fill in your values, then add
//   #include "config.h"
// near the top of zero_buttons.ino (and remove the inline defaults).

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Deployed API base URL (https), no trailing slash.
#define API_BASE "https://your-api.example.com"

// Device token from the dashboard Devices page (shown once on creation).
#define DEVICE_TOKEN "zd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
