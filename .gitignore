node_modules/
dist/
.next/
out/
build/

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# misc
.DS_Store
.turbo/
coverage/
*.tsbuildinfo

# agent tooling — personal/local settings and vendored skills.
# Skills are restorable from skills-lock.json, so we track the lock, not the content.
.claude/settings.local.json
.claude/skills/*
.agents/skills/
# ...except first-party skills authored for this repo, which aren't in the lock.
!.claude/skills/pr/
